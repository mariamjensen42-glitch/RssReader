# 2026-08-10

## Fluent Reader → Tauri v2 最基础迁移完成

### 已完成
- Rust 后端: settings.rs (JSON文件持久化, 20+ 设置字段) + lib.rs (全部 Tauri commands)
- 窗口配置: 1200x700, min 992x600, 原生装饰
- 前端依赖: React 19.1, FluentUI v9 纯, Zustand 状态管理 (替代原 Redux)
- 类型定义: schema-types.ts 直接复制
- IPC 桥接: bridges/settings.ts + bridges/utils.ts (ipcRenderer → invoke)
- 主题系统: theme.ts 纯 v9 (移除 v8), 深色/浅色切换
- 组件: Root (FluentProvider) → Nav (标题栏) → Page (占位) + Menu (侧栏, 静态菜单项)
- 全局样式: global.css (CSS 变量沿用原项目)

### 文件结构
```
src/
  store.ts              # Zustand store (app + page state)
  schema-types.ts       # 原项目类型
  bridges/settings.ts   # Tauri invoke 桥接
  bridges/utils.ts      # Tauri invoke 桥接
  components/
    root.tsx            # FluentProvider + 主题
    nav.tsx             # 导航栏
    menu.tsx            # 侧栏菜单(静态: 搜索 + 所有文章)
    page.tsx            # 欢迎占位
    utils/              # FlatButton, theme, hooks
  scripts/settings.ts   # getFontFamilyForLocale
  styles/global.css

src-tauri/src/
  lib.rs                # 30+ Tauri commands + AppState
  settings.rs           # SettingsStore (JSON持久化)
  main.rs               # 入口
```

### 关键决策
- Zustand 替代 Redux（更轻量，无 redux-thunk ESM 兼容问题）
- React 保持 19.1（不用降级到 17）
- 纯 FluentUI v9（移除 v8 ThemeProvider）
- 设置存储: `{config_dir}/rustrssreader/settings.json`

### 下一步
- RSS 解析 (reqwest + feed-rs)
- 数据库层
- Feed 组件 (卡片/列表视图)
- Article 阅读视图
- Settings 面板

## UI 改进 (第二轮)
- Nav: 完整功能按钮 (Refresh/MarkRead/Notifications/View/Settings) 用 v9 图标
- 布局: `.main` 容器 + `menu-on` 推右 280px
- 深色模式: 全局 CSS 变量全覆盖 (--white, --neutralLight, --neutralLighterAlt 等)
- Page: 空状态 "No articles yet" + 提示
- 菜单项: unreadCount 显示

## RSS 核心功能实现 (第三轮)

### Rust 后端
- 新增依赖: rusqlite(bundled), reqwest(blocking), rss 2.x, chrono, scraper, url
- database.rs: SQLite CRUD (feeds/articles 两表), UNIQUE(feed_id,guid) 去重, 全文搜索, 已读/收藏切换
- lib.rs: 12 个新 Tauri commands:
  - Feeds: add_feed, remove_feed, update_feed, list_feeds, refresh_feed, refresh_all_feeds
  - Articles: get_articles, get_article, mark_read, mark_all_read, toggle_star, search_articles
- RSS 解析: Channel::read_from + 逐 item 入库
- DB 路径: `{data_dir}/rustrssreader/rss.db`

### 前端
- bridges/feeds.ts: FeedRow/ArticleRow 类型 + feedsBridge (全部 invoke 封装)
- store.ts 重构: 新增 feeds/articles/currentArticle 状态, 新增 10+ data actions
- main.tsx: 挂载 window.feeds bridge
- components/page.tsx: 卡片视图 + 列表视图 (ViewType.Cards/List 切换), 空状态/加载态, 搜索栏
- components/ArticleDetail.tsx: 文章详情 (HTML 渲染, 已读/收藏/原文打开), 自动标已读
- components/menu.tsx: 动态 Feed 列表 (按 group 分组, unread badge, Starred 入口)
- components/nav.tsx: 按钮全部接线 (Refresh All, Mark All Read, View toggle), 自动刷新定时器
- components/settings.tsx: Sources Tab 实现 (添加/删除/刷新 Feed)

### 验证
- tsc --noEmit: pass
- pnpm build: pass
- cargo check: pass

## 9大功能补完 (第四轮)

### 新功能
- 滚动标记已读: ArticleDetail 监听滚动容器(data-scroll-container), 距底部80px自动标已读
- 字体大小调节: Settings Slider 12-28px, CSS变量 --articleBodySize/--articleTitleSize 全局生效
- 未读筛选: Page 列表顶部 ToggleButton "Unread only", store.loadArticles opts.onlyUnread
- 刷新间隔: Settings Select 5/10/15/30/60min, Nav 每10s轮询最新设置值
- RSS自动发现: Rust discover_feeds命令(解析HTML <link>标签 + HEAD探测常见路径), Settings一键添加
- Feed右键菜单: Menu Rename/Mark All Read/Refresh/Unsubscribe + 内联编辑
- 阅读进度记忆: Page scrollPositions Ref, 切换文章保存scrollTop, 返回恢复
- 导出文章: Rust export_article命令 (md/html), ArticleDetail MD/HTML导出按钮
- 通知: tauri-plugin-notification已添加(cargo依赖+capability, 待前端触发逻辑)

### 新增Rust命令
- discover_feeds(website_url) → Vec<{url,title,type}>
- export_article(id, format) → String (md|html)

### 编译验证
- tsc --noEmit: pass
- cargo check: pass (2个warning, 非阻塞)

## 代码审查问题修复 (第六轮)

### Rust 修复
- database.rs: `new()` → `Result<Self, String>`, 移除 `expect()` 和 `get_feed_meta()` 死代码
- settings.rs: 2 处 `unwrap()` → `unwrap_or_default()`
- lib.rs: OPML 导出添加 `xml_escape()` 函数, Database::new() 调用点适配
- Cargo.toml: 添加 `[lints.rust]` (`unsafe_code = "forbid"`) + `[lints.clippy]` (`unwrap_used/expect_used = "warn"`)
- HTTP 超时: `fetch_feed` 和 `discover_feeds` 已有 15s/10s 超时, 无需额外修改

### TypeScript 修复
- store.ts: `contextMenu.event/target` 的 `any` → `string`/`unknown`
- 新增 `src/components/ErrorBoundary.tsx`, 包裹 `<App />` 防止白屏崩溃
- 新建 `src/types/global.d.ts` 统一 Window 类型声明, 从 3 个 bridge 文件移除分散的 `declare global`
- main.tsx: 移除 `window.fontList` 死代码

### 验证
- tsc --noEmit: pass
- cargo check: pass

## Windows 亚克力/云母材质 (第七轮)

### 配置修改
- tauri.conf.json: `transparent: true` + `windowEffects: { effects: ["mica"] }`, 移除 `titleBarStyle: "Overlay"`（macOS only）
- global.css: `html,body` 和 `.main` 背景改为 `transparent`，Mica 效果透出
- `.main::before` 渐变遮罩降低 `opacity: 0.6`

### 效果说明
- 窗口默认使用 **Mica**（Win11 风格，桌面壁纸微模糊）
- 运行时切换 API 因 `WindowEffectsConfig` 私有，暂通过 config 静态配置
- Nav 栏保持已有 `backdrop-filter: blur(30px) saturate(125%)` 毛玻璃
- 侧栏/内容卡片保持实色背景保证可读性

### 默认视图改为 Magazine
- store.ts: `viewType: ViewType.Cards` → `ViewType.Magazine`
- settings.rs: `view: 0u8` → `2u8`

### Unread only 按钮移至 Nav 栏
- store.ts: `onlyUnread` 从本地 useState 提升到 Zustand store + `toggleUnread` action
- nav.tsx: 在 MarkAllRead 和 View 之间加 Filter 图标按钮，激活时蓝色高亮
- page.tsx: 移除本地 `onlyUnread` state 和工具栏行，文章计数保留为右侧小字
- store 内所有 `loadArticles` 调用统一传递 `onlyUnread` 状态
- tsc --noEmit: pass

### 标签功能 (第八轮)
- 数据库: 新增 `tags` + `article_tags` 表 (M2M), `TagRow` 结构体 + 5 个 CRUD 方法
- Rust 命令: `add_tag`, `remove_tag`, `get_article_tags`, `get_all_tags`, `get_articles_by_tag`
- 前端桥接: `TagRow` 类型 + 5 个 IPC 方法
- Store: `tags`, `currentArticleTags`, `tagId` 状态 + 5 个 tag action
- ArticleDetail: 标签 chips 显示 + 内联添加输入框 + 删除按钮
- Menu 侧栏: Tags 分区，显示标签名+文章数，点击筛选文章
- tsc --noEmit + cargo check: pass

### 分组维护优化 (第九轮)
- Group 输入改为原生 `<select>`（已有分组 + "+ New group..." → Input 手打），FluentUI Combobox 有运行时崩溃
- Settings→Feeds: feed 列表按分组归类展示，每组内 feed 右侧 `<select>` 快速改分组
- Menu 右键菜单: 新增 "Move to Group" 区域，列出所有分组 + Ungrouped 选项，一键移动
- URL 输入框 flex 加宽到 240px
- tsc --noEmit: pass
